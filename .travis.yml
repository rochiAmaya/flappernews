language: node_js
node_js:
  - "0.12"

cache:
  directories:
    - node_modules

#    gulp config
before_script:
  - npm install -g gulp
  - npm install -g bower
  - bower install -f
script:
  - gulp
